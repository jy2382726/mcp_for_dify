name: mcp_for_dify
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - fastapi
    - uvicorn[standard]
    - mcp
    - loguru
    - pydantic
    - pydantic-settings
    - pytest
    - pytest-asyncio
    - httpx
